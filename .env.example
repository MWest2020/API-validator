# API Credentials - Replace with your actual credentials
# IMPORTANT: Never hardcode these values in your test scripts
# Always load them from this .env file to keep sensitive data secure
API_USERNAME=username@example.com
API_PASSWORD=your_secure_password
API_TOKEN=your_token_if_needed

# API Configuration
API_TIMEOUT=30
API_MAX_RETRIES=3

# Logging Configuration
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
LOG_FILE=api_test.log

# SECURITY REMINDER:
# 1. Keep this file out of version control (.gitignore)
# 2. Restrict file permissions to only the necessary users
# 3. Never share this file with credentials included
# 4. Consider using a credentials management service in production 